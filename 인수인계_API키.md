# AI 사진 분석 — 키만 넣으면 켜집니다

배선은 전부 끝나 있습니다. **API 키를 한 번 넣고 재배포하면 바로 작동합니다.**

## 넣는 법 (5분)

1. 구글 시트 → 확장 프로그램 → **Apps Script**
2. 왼쪽 **톱니바퀴(프로젝트 설정)** 클릭
3. 맨 아래 **스크립트 속성** → **속성 추가**
   - 이름: `ANTHROPIC_API_KEY`
   - 값: `sk-ant-...` (발급받은 키)
4. 저장
5. **배포 → 배포 관리 → 연필 → 버전: 새 버전 → 배포**

키는 스크립트 안에만 있습니다. 웹페이지로는 나가지 않습니다.

## 켜졌는지 확인

관리자 화면 → 응답 펼치기 → **공간 분석** 칸.

- 키 넣기 전: `AI 분석 대기 — 앱스스크립트에 API 키를 넣으면 이 버튼이 살아납니다`
- 키 넣은 후: **[AI로 사진 분석]** 버튼이 눌리는 상태로 바뀜 (사진이 1장 이상 있어야 함)

## 무슨 일이 일어나나

1. 담당자가 사진을 넣음 → 드라이브 저장 + 시트 칸에 표시
2. **[AI로 사진 분석]** 클릭
3. Apps Script가 드라이브에서 사진을 꺼내 Claude에 보냄 (20~60초)
4. 돌려받은 값이 시트 `공간분석` 열에 저장되고, **추천 5점이 즉시 다시 계산됨**
5. 이어서 작품 5점 각각의 코멘트도 생성

## 결과값 규격 (프롬프트 다듬을 때 기준)

추천 계산에 **직접 쓰이는 네 개** — 이게 틀리면 추천 작품이 엉뚱해집니다:

| 키 | 값 |
|---|---|
| `wallIdx` | 0=1m미만 / 1=1~2m / 2=2~3m / 3=3m이상 ← **가장 중요** |
| `lightIdx` | 0=종일 자연광 / 1=오후만 / 2=자연광 거의 없음 / 3=스팟조명 |
| `roomIdx` | 0=화이트+원목 / 1=그레이·블랙 / 2=베이지 / 3=색·패턴 많음 |
| `toneShift` | -30~30 (실내에 색이 많으면 음수) |

설명용 문장 7개: `wall` `light` `style` `wallWidth` `placement` `advice` `quality`

## 손볼 자리

`구글시트_AppsScript.gs` 안에서:

- `AI_MODEL` — 기본 `claude-sonnet-5`. 더 정밀하게 하려면 `claude-opus-5`
- `AI_MAXTOK` — 응답 길이 상한 (기본 1200)
- `aiAnalyzeSpace()` 안의 `prompt` — 공간 분석 프롬프트
- `aiWorkComments()` 안의 `prompt` — 작품별 코멘트 프롬프트

프롬프트만 고치고 재배포하면 됩니다. 화면 코드는 건드릴 필요 없습니다.

## 비용·시간

- 사진 3장 + 분석 1회 ≈ 수십 원 수준
- 한 번에 20~60초. Apps Script 실행 한도(6분) 안에서 끝납니다.
- 분석과 코멘트는 각각 한 번씩, 총 2회 호출됩니다.
