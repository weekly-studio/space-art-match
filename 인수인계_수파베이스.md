# 수파베이스 연동 인수인계

## 0. 먼저 읽을 것 — 파일 구조

- **원본**: `공간작품매칭테스트.html` (단일 파일 · 약 4MB · 이미지가 base64로 박혀 있음)
- **배포본**: `index.html` — `deploy.sh`가 원본을 복사해 만드는 **산출물**
- 서버: `구글시트_AppsScript.gs` (구글 시트에 붙은 Apps Script 웹앱)

> `index.html`은 직접 고치지 마세요. 배포할 때마다 원본으로 덮어써집니다.
> 반드시 `공간작품매칭테스트.html`만 고칩니다.

배포: `./deploy.sh "커밋 메시지"` → 빌드번호 기록 + index.html 갱신 + git push.

---

## 1. 할 일 한 줄 요약

현재 **구글 시트가 메인**입니다. 이걸 **수파베이스가 메인**으로 바꾸고,
구글 시트는 **백업(쓰기만)** 으로 남깁니다.

---

## 2. 고칠 곳은 한 군데입니다

원본 파일에서 `데이터 계층` 주석을 찾으세요. (`const SHEETS = {` 바로 위)

화면 코드는 저장소를 직접 부르지 않습니다. 전부 `API.xxx()`를 거칩니다.
따라서 **`SUPA` 객체 안의 함수만 채우고 아래 두 줄만 바꾸면 끝납니다.**

```js
let PRIMARY = "supabase";   // 읽고 쓰는 곳 (정답)
let MIRROR  = "sheet";      // 쓰기만 한 벌 더 (백업, 실패해도 무시)
```

`SUPA.url`, `SUPA.anonKey`에 프로젝트 주소와 anon 키를 넣으면 `ready()`가 true가 됩니다.

### 채워야 할 함수와 돌려줄 모양

| 함수 | 하는 일 | 돌려줄 값 |
|---|---|---|
| `checkGuest(name, tail)` | 초대 명단에서 성함 찾기 | `{ok:true, name, phone, code, by, at}` / `{ok:false, reason?, nameHit?}` |
| `submitResponse(rec)` | 손님 응답 저장 | `{ok:true}` / `{ok:false, reason}` |
| `createInvite(v)` | 초대 발급 기록 | `{ok:true}` |
| `listInvites()` | 초대 명단 | `[{at, name, code, by, memo, done}]` |
| `listResponses()` | 응답 목록 | 아래 "응답 한 건의 모양" 배열 |
| `updateInvite(p)` / `deleteInvite(p)` | 초대 수정·삭제 (`p.inviteCode` 기준) | `{ok:true}` / `{ok:false, error:"사람이 읽을 메시지"}` |
| `updateResponse(p)` / `deleteResponse(p)` | 응답 수정·삭제 (`p.id`, `p.at` 기준) | 〃 |
| `putPhoto(p)` | 사진 1장 저장 (`p.id`, `p.slot` 0~2, `p.photo` = data URL) | `{ok:true, url:"공개 URL"}` |
| `deletePhoto(p)` | 사진 1장 삭제 (`p.id`, `p.slot`) | `{ok:true}` |
| `adminAuth(code)` | 담당자 접속 코드 확인 | `{auth:true}` / `null` |

`aiStatus` / `aiSpace` / `aiComment`는 **건드리지 마세요.** AI 분석은 계속 Apps Script가 맡습니다(그쪽에만 API 키가 있습니다).

### 응답 한 건의 모양 (`listResponses`가 돌려줄 것)

```js
{
  id:"...", at:"ISO 문자열", name:"", phone:"", inviteCode:"", by:"",
  type:"담백한 서정", tone:30, form:25, density:20, motiveLabel:"감정",
  answers:[12개 문자열],      // 순서: 공간·규격·조도·실내 톤·정서·화면·인상·색·재료·동기·예산·단계
  memo:"", photos:["공개 URL", ...최대 3],
  picks:["작품id", ...5], space:"공간분석 JSON 문자열 또는 빈 문자열"
}
```
이 배열은 `rowFromSheet()`가 그대로 받아 화면 모양으로 바꿉니다. **키 이름을 바꾸지 마세요.**

---

## 3. 표 구조 (지금 시트 열과 1:1)

```
responses
  id            text   pk
  at            timestamptz
  name          text
  phone         text
  invite_code   text
  by            text            -- 담당자
  type          text            -- 취향 유형 이름
  tone          int             -- 채도 0~100
  form          int             -- 형상 0~100
  density       int             -- 밀도 0~100
  motive_label  text
  answers       jsonb           -- 12문항 답변
  memo          text
  photos        text[]          -- 공개 URL 최대 3
  picks         text[]          -- 담당자가 확정한 작품 id 5
  space         jsonb           -- 공간 분석 결과 (아래 4번)

invites
  code   text pk
  at     timestamptz
  name   text
  phone  text
  by     text
  memo   text
  url    text
  status text
```

사진: Storage 버킷 하나(공개 읽기). 경로 예) `responses/<id>/0.jpg`

---

## 4. 공간 분석(`space`) — 건드리지 말 것

AI 또는 담당자가 채우는 값입니다. 추천 계산이 이 네 값을 직접 씁니다.

```json
{ "wallIdx":0~3, "lightIdx":0~3, "roomIdx":0~3, "toneShift":-30~30,
  "wall":"문장", "light":"문장", "style":"문장", "wallWidth":"문장",
  "placement":"문장", "advice":"문장", "quality":"문장",
  "by":"ai 또는 curator", "at":"ISO" }
```

그대로 넣고 그대로 꺼내 주시면 됩니다.

---

## 5. 지켜야 할 규칙

1. **읽기는 PRIMARY 한 곳에서만.** 양쪽에서 읽으면 관리자 화면에 같은 손님이 두 번 뜹니다.
2. **시트 칸을 손으로 고치지 않기.** 수정은 관리자 화면에서만 (안 그러면 두 곳이 어긋납니다).
3. `syncResponses()`는 "PRIMARY가 정답"이라는 전제로 로컬 목록을 덮어씁니다. 그대로 두세요.
4. 응답 id는 지금 시트 행 번호(`s12`)입니다. 수파베이스로 옮기면 uuid가 되는데, **화면은 id를 문자열로만 다루므로 문제없습니다.** 단 옛 데이터를 옮길 때 id 매핑만 주의하세요.

---

## 6. 확인 방법

1. 원본 파일을 브라우저로 직접 엽니다 (`file://...공간작품매칭테스트.html`)
2. 주소 뒤에 `#admin` → 접속 코드 `1440`
3. 응답 목록이 뜨고, 응답을 펼쳐 사진 넣기·공간 분석·추천 5점이 동작하면 성공
